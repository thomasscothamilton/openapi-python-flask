apiVersion: pubsub.cnrm.cloud.google.com/v1beta1
kind: PubSubTopic
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: thomasscothamilton
  name: {{ .Values.nameOverride | default .Chart.Name }}-topic
spec:
  projectRef:
    external: projects/thomasscothamilton
