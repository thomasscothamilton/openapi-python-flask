apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openapi-python-flask.fullname" . }}-test-connection"
  labels:
    {{- include "openapi-python-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openapi-python-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
