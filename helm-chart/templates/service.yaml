apiVersion: v1
kind: Service
metadata:
  name: {{ include "openapi-python-flask.fullname" . }}
  labels:
    {{- include "openapi-python-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openapi-python-flask.selectorLabels" . | nindent 4 }}
